# Money Management

## Installation
Please create a `.env` file as shown in `.env.example`

Install all required libraries `pip install -r requirements.txt`

Run migrate commands if necessary. `python manage.py migrate`

Run the project `python manage.py runserver`